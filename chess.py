def binary_search(numbers, target):
    if target:
        return target
    else:
        return index

        index = -1

numbers = [1, 2, 3, 4, 5]
target = numbers.index(7)
print(binary_search(numbers,target))
